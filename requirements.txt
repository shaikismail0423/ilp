streamlit>=1.30.0
matplotlib>=3.7.0

# Optional — used only by core/benchmark.py (run via demo.py / run_benchmark.py).
# When missing, the benchmark falls back to pure-Python t-test / Wilcoxon.
scipy>=1.10.0

# Optional — used only by demo.py for its comparison tables.
pandas>=2.0.0
